[package]
name = "chouten"
version = "0.1.0"
edition = "2021"
description = "A cli for testing and creating chouten modules"
license = "MIT"

[dependencies]
reqwest = { version = "0.11", features = ["json", "blocking"] }
tokio = { version = "1", features = ["full"] }
v8 = "0.92.0"
